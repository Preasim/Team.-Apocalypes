import { WorkArrayType } from "./AddWork";
import { CreateBoard } from "@/app/fetch/CreateWorkListFetch/POST";

export const setCreateWork = async function () {
  try {
  } catch (err) {
    // 에러 핸들러 작성
  }
  // const createBoardData = await CreateBoard();  // 멤버 id 필요
  // setAddWorkArray(createBoardData);
};
